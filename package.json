{
  "name": "neon-drift",
  "version": "1.0.0",
  "description": "NEON DRIFT · 霓虹幸存者 —— 纯原生 HTML5 Canvas roguelite 生存射击小游戏",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "trap 'kill 0' EXIT INT TERM; python3 -m http.server 8000 --bind 127.0.0.1 & sleep 1 && open http://localhost:8000 && wait",
    "serve:dist": "python3 -m http.server 8000 --directory dist"
  },
  "license": "MIT"
}
